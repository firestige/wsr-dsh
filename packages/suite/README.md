# WSR suite

`dsh-wsr` is a compatible composition bundle. It installs and activates `dsh-wsr-execution@^0.2.3` and `dsh-wsr-studio@^0.1.2`; it has no runtime entry point, display name, or UI registration of its own.
