name: Release Promote

on:
  workflow_dispatch:

permissions: {}

jobs:
  not-ready:
    name: Publishing is not enabled yet
    runs-on: ubuntu-latest
    permissions: {}
    steps:
      - name: Fail closed until the qualified promotion adapter lands
        shell: bash
        run: |
          echo "The wsr-dsh release authority is not production-ready."
          exit 1
